---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: instance
    image: jrei/systemd-ubuntu
    groups:
      - zookeeper
    privileged: True
    command: /sbin/init
provisioner:
  name: ansible
verifier:
  name: ansible
